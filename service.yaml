apiVersion: v1
kind: Service
metadata:
  name: orion-service
  namespace: ceph
  labels:
    name: orionservice-nodeport-svc
spec:
  type: NodePort
  ports:
    - port: 1026
      nodePort: 30180
      name: http
    - port: 443
      nodePort: 31443
      name: https
  selector:
    name: broker
